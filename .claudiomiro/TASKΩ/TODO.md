Fully implemented: NO

## Context Reference

**For complete environment context, read these files in order:**
1. `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/AI_PROMPT.md` - Universal context (tech stack, architecture, conventions)
2. `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/TASK.md` - Task-level context (what this task is about)
3. `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/PROMPT.md` - Task-specific context (files to touch, patterns to follow)

**You MUST read these files before implementing to understand:**
- Tech stack: Swift 5.x + SpriteKit + Swift Package Manager
- Project structure: Sources/BugDefense/, Tests/BugDefenseTests/
- Coding conventions: Swift naming, CaseIterable enums, vector-based movement
- Related code examples with file:line references
- Integration points: Bug movement system, MapManager, GameScene
- Grid system: 20x15 tiles, safe zone x:1-18 y:1-13, house at (10,7)

**DO NOT duplicate this context below - it's already in the files above.**

## Implementation Plan

- [ ] **Validation 1 — Code Completeness Verification**
  - **What to do:**
    1. Read `Sources/BugDefense/MapConfiguration.swift` completely (lines 1-631)
    2. Count MapType enum cases (lines 5-24) - verify exactly 20 existing maps (map1 through map20)
    3. Verify each map has corresponding path method (e.g., map1Path, map2Path, etc.)
    4. Verify roadPath switch statement (lines 42-64) includes all 20 cases
    5. Run `swift build` to confirm no compilation errors
    6. Run `swift test` to verify all existing tests pass
    7. Document any missing maps (should be 0, since TASKΩ validates completion)
    8. If fewer than 30 total maps exist (20 + 10 new), identify which TASK1-10 implementations are missing

  - **Context (read-only):**
    - `Sources/BugDefense/MapConfiguration.swift:1-631` — Complete map configuration system
    - `Sources/BugDefense/MapConfiguration.swift:5-24` — MapType enum with all cases
    - `Sources/BugDefense/MapConfiguration.swift:42-64` — roadPath switch statement
    - `Sources/BugDefense/MapConfiguration.swift:70-103` — expandPath algorithm (should not be modified)
    - `Sources/BugDefense/Bug.swift:240-302` — Bug movement and waypoint system (should not be modified)

  - **Touched (will modify/create):**
    - READ ONLY: `Sources/BugDefense/MapConfiguration.swift`
    - READ ONLY: All source files (verification only)

  - **Interfaces / Contracts:**
    - MapType enum: CaseIterable protocol must include all map cases
    - MapType.allCases: Auto-generated by CaseIterable, includes all enum cases
    - MapType.random(): Uses allCases.randomElement() to select maps
    - Each map case: Must have corresponding path method returning [GridPosition]
    - roadPath computed property: Switch statement must handle all enum cases

  - **Tests:**
    Type: Verification checks (no new tests written in this task)
    - Compilation test: `swift build` succeeds with zero errors
    - Existing tests: `swift test` passes all tests
    - Count verification: Exactly 30 MapType cases (20 + 10 new)
    - Switch exhaustiveness: Compiler verifies all cases handled

  - **Migrations / Data:**
    N/A - No data changes (validation only)

  - **Observability:**
    N/A - Validation task does not add logging

  - **Security & Permissions:**
    N/A - No security concerns (validation only)

  - **Performance:**
    - Verification should complete in < 2 minutes
    - Build time: < 30 seconds (incremental build)
    - Test execution: < 10 seconds (unit tests only)

  - **Commands:**
    ```bash
    # Build project to verify compilation
    swift build

    # Run all tests
    swift test

    # Count map cases (should be 30)
    grep "case map[0-9]" Sources/BugDefense/MapConfiguration.swift | wc -l

    # Verify no compilation warnings
    swift build 2>&1 | grep -i warning
    ```

  - **Risks & Mitigations:**
    - **Risk:** Some TASK1-10 implementations incomplete (fewer than 30 maps total)
      **Mitigation:** Document missing tasks, return to incomplete tasks for implementation
    - **Risk:** Compilation errors from malformed map definitions
      **Mitigation:** Run swift build first, identify and fix syntax errors
    - **Risk:** Tests fail due to invalid paths (out of bounds, don't reach house)
      **Mitigation:** Review test output, fix invalid path definitions

- [ ] **Validation 2 — Requirements Traceability Matrix**
  - **What to do:**
    1. Open AI_PROMPT.md and read acceptance criteria (lines 121-167)
    2. For each requirement, locate implementation in codebase
    3. Verify Map Design Requirements (lines 126-133):
       - Count new maps: grep "case map2[1-9]\|case map30" MapConfiguration.swift
       - Inspect each map's path for distinct pattern (visual variety)
       - Verify all paths stay in safe zone (x:1-18, y:1-13): check min/max coordinates
       - Verify paths start at edge: first GridPosition has x=1 or x=18 or y=1 or y=13
       - Verify paths end at house: last GridPosition = (10, 7)
       - Verify no overlap with house except final destination
       - Measure path lengths: count GridPosition array elements (expandPath result)
       - Classify difficulty: short paths = easy, long complex paths = hard
    4. Verify Waypoint System Requirements (lines 139-148):
       - Confirm waypoints stored as [GridPosition] arrays
       - Test path expansion: expandPath fills intermediate tiles correctly
       - Review Bug.setPath() (Bug.swift:240-252) - assigns path, positions at first waypoint
       - Review Bug.update() (Bug.swift:254-302) - vector movement, waypoint snap, path advance
       - Confirm final waypoint equals house position for all maps
    5. Verify Visual Requirements (lines 150-156):
       - Review GameScene.drawGrid() - road tiles render on roadPath positions
       - Manual test (if possible): Run game, check visual rendering
    6. Verify Integration Requirements (lines 153-159):
       - Test MapType.random(): call in Swift REPL or add temporary print in GameScene
       - Review MapManager.selectRandomMap() implementation
       - Review GameScene.spawnBug() (lines 488-504) - calls MapManager.getCurrentRoadPath()
       - Review GameScene.canPlaceStructure() - blocks tower placement on roads
    7. Create traceability matrix table mapping each requirement to verification result

  - **Context (read-only):**
    - `.claudiomiro/AI_PROMPT.md:121-167` — Complete acceptance criteria
    - `Sources/BugDefense/MapConfiguration.swift:5-631` — All map implementations
    - `Sources/BugDefense/Bug.swift:240-302` — Waypoint movement system
    - `Sources/BugDefense/GameScene.swift:488-504` — Bug spawning with path assignment
    - `Sources/BugDefense/GameScene.swift:237-304` — Grid rendering system

  - **Touched (will modify/create):**
    - CREATE: `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/TRACEABILITY_MATRIX.md`
    (Note: Create this file to document verification results)

  - **Interfaces / Contracts:**
    - Acceptance criteria contract: Every criterion must have "✅ Verified" or "❌ Failed" status
    - Traceability format: | Requirement | Location | Status | Notes |

  - **Tests:**
    Type: Manual verification with documentation
    - Each acceptance criterion: Locate implementation, verify correctness
    - Edge case: Map paths stay in bounds (test min/max coordinates)
    - Edge case: Paths reach house (test last waypoint)
    - Documentation: All findings recorded in TRACEABILITY_MATRIX.md

  - **Migrations / Data:**
    N/A - Documentation only

  - **Observability:**
    - Document findings in TRACEABILITY_MATRIX.md
    - Include file:line references for each requirement

  - **Security & Permissions:**
    N/A - No security concerns

  - **Performance:**
    - Manual verification: 15-30 minutes
    - Automated checks (grep, count): < 1 minute

  - **Commands:**
    ```bash
    # Count new maps (should be 10)
    grep -E "case map(2[1-9]|30)" Sources/BugDefense/MapConfiguration.swift | wc -l

    # Verify all paths end at house (10, 7)
    # Manual inspection required for each mapXXPath method

    # Check safe zone boundaries
    # Manual inspection of each path's GridPosition coordinates

    # Generate traceability matrix (manual documentation)
    # Create .claudiomiro/TASKΩ/TRACEABILITY_MATRIX.md
    ```

  - **Risks & Mitigations:**
    - **Risk:** Manual verification misses edge cases
      **Mitigation:** Systematically check each criterion, document method used
    - **Risk:** Requirements interpretation ambiguous
      **Mitigation:** Reference AI_PROMPT.md exact wording, ask for clarification if needed
    - **Risk:** Incomplete requirement coverage
      **Mitigation:** Use checklist format, ensure all criteria addressed

- [ ] **Validation 3 — Integration Testing and System Cohesion**
  - **What to do:**
    1. Verify MapType.allCases includes all 30 maps:
       - Add temporary test: `print("Total maps: \(MapType.allCases.count)")`
       - Expected output: "Total maps: 30"
    2. Test random map selection:
       - Run game or add test: call MapType.random() 100 times
       - Verify new maps (map21-30) appear in random selection
       - Track distribution (should be roughly uniform)
    3. Test map switching mid-game:
       - Review GameScene.swift:1287-1290 (tier progression map changes)
       - Verify MapManager.selectRandomMap() called at tier boundaries
       - Confirm grid redraw triggered (GameScene.redrawGrid())
    4. Test bug path assignment:
       - Review GameScene.spawnBug() implementation (lines 488-504)
       - Verify all bugs receive MapManager.getCurrentRoadPath()
       - Confirm bugs position at first waypoint via Bug.setPath()
    5. Test tower placement blocking:
       - Review GameScene.canPlaceStructure() (lines 826-856)
       - Verify road positions blocked for all maps
       - Test with multiple maps to ensure consistency
    6. Run regression tests:
       - Execute `swift test` to verify no existing functionality broken
       - Check for test failures or new warnings
       - Verify existing maps (map1-20) still work correctly
    7. Manual playtesting (if possible):
       - Run game, select 3-5 different new maps
       - Observe bugs spawn at path start
       - Confirm bugs follow path precisely to house
       - Check road tiles render correctly (brown dirt)
       - Verify towers cannot be placed on roads
       - Test map switching at tier progression

  - **Context (read-only):**
    - `Sources/BugDefense/MapConfiguration.swift:5-24` — MapType enum (CaseIterable)
    - `Sources/BugDefense/MapConfiguration.swift:36-38` — MapType.random() implementation
    - `Sources/BugDefense/GameScene.swift:488-504` — spawnBug() method
    - `Sources/BugDefense/GameScene.swift:826-856` — canPlaceStructure() method
    - `Sources/BugDefense/GameScene.swift:1287-1290` — Tier progression logic
    - `Sources/BugDefense/GameScene.swift:305-314` — redrawGrid() method
    - `Tests/BugDefenseTests/BugDefenseTests.swift:125-185` — Existing bug path tests

  - **Touched (will modify/create):**
    - READ ONLY: All source files (integration testing)
    - OPTIONAL: Add temporary debug prints for verification (remove after)

  - **Interfaces / Contracts:**
    - MapType.allCases: Must return all 30 map cases
    - MapType.random(): Must have equal probability for all maps
    - MapManager.getCurrentRoadPath(): Returns [GridPosition] for current map
    - Bug.setPath(): Positions bug at first waypoint, sets pathIndex
    - GameScene integration: Spawning, rendering, blocking all work correctly

  - **Tests:**
    Type: Integration tests + manual testing
    - Integration: MapType.allCases.count == 30
    - Integration: MapType.random() returns new maps
    - Regression: All existing tests pass (`swift test`)
    - Manual: Bugs follow new map paths correctly
    - Manual: Road tiles render for all new maps
    - Manual: Tower placement blocked on all roads

  - **Migrations / Data:**
    N/A - No data changes

  - **Observability:**
    - Existing print statements in GameScene.spawnBug() (lines 496, 499, 503)
    - These logs show road path assignment and waypoint counts
    - Monitor console output during manual testing

  - **Security & Permissions:**
    N/A - No security concerns

  - **Performance:**
    - MapType.random(): O(1) array access
    - Path assignment: O(n) where n = path length (typically 10-50)
    - Integration test suite: < 10 seconds
    - Manual playtesting: 10-15 minutes for 3-5 maps

  - **Commands:**
    ```bash
    # Run all tests (regression check)
    swift test

    # Run specific test file
    swift test --filter BugDefenseTests

    # Build and run game for manual testing (macOS)
    swift build
    open .build/debug/BugDefense.app

    # Check for runtime warnings/errors
    swift build 2>&1 | grep -E "warning|error"
    ```

  - **Risks & Mitigations:**
    - **Risk:** New maps cause runtime crashes (nil paths, out of bounds)
      **Mitigation:** Test each map individually, catch exceptions, fix invalid paths
    - **Risk:** Existing maps broken by changes
      **Mitigation:** Run full test suite, compare with baseline behavior
    - **Risk:** Performance degradation with 30 maps vs 20
      **Mitigation:** Profile map selection, verify O(1) complexity maintained
    - **Risk:** Visual glitches in rendering
      **Mitigation:** Manual testing, check grid redraw for all maps

- [ ] **Validation 4 — Pattern Variety and Quality Assessment**
  - **What to do:**
    1. For each new map (map21-30), analyze path pattern:
       - Load map path from MapConfiguration.swift
       - Visualize on 20x15 grid (sketch or ASCII art)
       - Classify pattern type (zigzag, spiral, S-curve, loop, etc.)
       - Measure path complexity: count turns, direction changes
       - Calculate path length: total GridPosition count after expansion
    2. Verify visual distinctness:
       - Compare each new map pattern against others
       - Ensure no two maps have identical or very similar patterns
       - Check for pattern variety: straight, curved, diagonal, complex
    3. Assess difficulty distribution:
       - Short direct paths (< 20 tiles) = Easy
       - Medium winding paths (20-40 tiles) = Medium
       - Long complex paths (> 40 tiles) = Hard
       - Verify mix across difficulty levels (not all easy or all hard)
    4. Validate gameplay balance:
       - Longer paths give players more time to build towers = easier
       - Complex paths with many turns = more tower placement opportunities
       - Short straight paths = harder, less build time
       - Verify difficulty aligns with path characteristics
    5. Check aesthetic quality:
       - Paths should look intentional, not random
       - Patterns should be recognizable (e.g., "looks like a figure-8")
       - Avoid awkward or nonsensical path shapes
    6. Document findings:
       - Create table: Map | Pattern Type | Length | Difficulty | Notes
       - Include ASCII visualization or description for each map

  - **Context (read-only):**
    - `Sources/BugDefense/MapConfiguration.swift:115-631` — All map path definitions
    - `.claudiomiro/AI_PROMPT.md:95-99` — Pattern examples (Winding, Straight, Spiral, Maze)
    - `.claudiomiro/AI_PROMPT.md:261-265` — Design guidelines (variety, balance, visual appeal)
    - `Sources/BugDefense/MapConfiguration.swift:118-137` — Map 1 example (winding road)
    - `Sources/BugDefense/MapConfiguration.swift:319-332` — Map 9 example (straight shot)

  - **Touched (will modify/create):**
    - CREATE: `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/PATTERN_ANALYSIS.md`
    (Document pattern analysis results)

  - **Interfaces / Contracts:**
    - Pattern classification: Each map assigned a descriptive pattern type
    - Difficulty rating: Easy / Medium / Hard based on path characteristics
    - Quality assessment: Pass / Fail for visual distinctness and aesthetic

  - **Tests:**
    Type: Qualitative analysis with documentation
    - Distinctness test: No two maps have identical patterns
    - Variety test: At least 5 different pattern types represented
    - Difficulty distribution: Mix of easy, medium, hard (not all one level)
    - Aesthetic test: Paths look intentional and recognizable

  - **Migrations / Data:**
    N/A - Analysis only

  - **Observability:**
    - Document analysis in PATTERN_ANALYSIS.md
    - Include visualizations or descriptions

  - **Security & Permissions:**
    N/A - No security concerns

  - **Performance:**
    - Pattern analysis: 20-30 minutes (manual inspection)
    - Path length calculation: Automated via expandPath count

  - **Commands:**
    ```bash
    # Count path length for each map (after expansion)
    # Manual calculation: inspect expandPath result for each mapXXPath

    # Example: Count waypoints in map1Path definition
    grep -A 20 "private var map1Path" Sources/BugDefense/MapConfiguration.swift | grep "GridPosition" | wc -l

    # Visualize path on grid (manual sketch or tool)
    # Plot each GridPosition on 20x15 grid
    ```

  - **Risks & Mitigations:**
    - **Risk:** Subjective assessment lacks objectivity
      **Mitigation:** Use quantitative metrics (path length, turn count) to support ratings
    - **Risk:** Pattern similarity not detected
      **Mitigation:** Visually compare all maps side-by-side, document differences
    - **Risk:** Difficulty rating inconsistent with gameplay
      **Mitigation:** Reference existing map examples for calibration

## Verification (global)

- [ ] Run targeted tests for validation:
      ```bash
      # Build project
      swift build

      # Run all tests
      swift test

      # Verify test output shows all tests passed
      # Expected: Test Suite 'All tests' passed

      # Count total maps
      grep "case map[0-9]" Sources/BugDefense/MapConfiguration.swift | wc -l
      # Expected: 30 (or 20 if new maps not yet implemented)

      # Check for compilation warnings
      swift build 2>&1 | grep -i warning
      # Expected: No warnings (or only acceptable warnings documented)
      ```
      **CRITICAL:** This is a validation task - do not run full-project modifications

- [ ] All acceptance criteria met (see section below)

- [ ] Code follows Swift conventions from AI_PROMPT.md:
      - MapType enum follows CaseIterable pattern
      - Path methods return [GridPosition] arrays
      - Naming convention: mapXXPath for path methods
      - Switch statement exhaustive (all enum cases handled)

- [ ] Integration points properly validated:
      - MapType.allCases includes all maps
      - MapType.random() can select any map
      - Bug.setPath() receives paths from MapManager
      - GameScene.spawnBug() assigns paths correctly
      - GameScene.canPlaceStructure() blocks roads

- [ ] No regressions detected:
      - All existing tests pass
      - Existing maps (map1-20) still work
      - Bug movement system unchanged
      - Grid rendering works for old and new maps

- [ ] System cohesion verified:
      - All components work together seamlessly
      - Map switching doesn't crash
      - Path expansion works for all maps
      - Visual rendering correct for all maps

## Acceptance Criteria

### Code Quality
- [ ] All map implementations present in MapConfiguration.swift
- [ ] Code compiles without errors: `swift build` succeeds
- [ ] Code follows Swift naming conventions and style
- [ ] No code style inconsistencies detected

### Functional Correctness
- [ ] All new map paths are valid:
  - Paths stay within safe zone (x:1-18, y:1-13)
  - Paths start at edge position (spawn point)
  - Paths end at house position GridPosition(x:10, y:7)
  - Path waypoints form continuous path (no gaps)
- [ ] All maps accessible via MapType.allCases (automatic via CaseIterable)
- [ ] MapType.random() can select any map including new ones
- [ ] Waypoint system works with all maps (existing Bug.setPath/update implementation)
- [ ] Integration points function correctly (spawning, rendering, blocking)

### Testing
- [ ] All existing tests pass: `swift test` shows 0 failures
- [ ] No new test failures introduced
- [ ] Test coverage adequate for validation (BugDefenseTests.swift:125-185 covers path system)

### Requirements Coverage (from AI_PROMPT.md:121-167)
**Map Design Requirements:**
- [ ] At least 10 new unique map layouts created (count >= 30 total maps)
- [ ] Each map has distinct visual pattern (verified in pattern analysis)
- [ ] All paths stay within safe zone boundaries (x:1-18, y:1-13)
- [ ] Paths start at edge positions (spawn points)
- [ ] Paths end at house position GridPosition(x:10, y:7)
- [ ] No path segment overlaps house except final destination
- [ ] Path lengths vary (mix of short and long paths)
- [ ] Mix of difficulty levels (easy straight, complex maze-like)

**Waypoint System Requirements:**
- [ ] Each map's waypoint array defines complete bug path ([GridPosition] arrays)
- [ ] Path expansion correctly fills intermediate tiles (expandPath algorithm unchanged)
- [ ] Bugs spawn at first waypoint (Bug.setPath positions at path[0])
- [ ] Bugs move sequentially through waypoints (Bug.update vector-based movement)
- [ ] Bugs snap to exact waypoint when within 2-point threshold (Bug.swift:280-284)
- [ ] Bugs advance to next waypoint after reaching target (pathIndex += 1)
- [ ] Final waypoint matches house position (all paths verified)

**Visual Requirements:**
- [ ] Road tiles (brown) render along entire path (GameScene.drawGrid)
- [ ] Grass tiles (green) render on non-path areas
- [ ] House position renders with distinct darker green
- [ ] No visual gaps in road paths
- [ ] Grid updates correctly when map changes

**Integration Requirements:**
- [ ] MapType.random() can select from all maps including new ones
- [ ] MapManager.selectRandomMap() works with expanded map pool
- [ ] Bugs receive correct path via setPath() when spawned
- [ ] Tower placement blocked on all road positions
- [ ] Path recalculation works when map changes mid-game
- [ ] Grid redraw reflects new map layout

**Edge Cases & Error Scenarios:**
- [ ] Diagonal segments: Bugs move smoothly without stair-stepping (vector movement)
- [ ] Sharp turns: Bugs navigate 90-degree turns precisely
- [ ] Long straight segments: Bugs don't drift off path
- [ ] Map switching: Changing maps mid-game doesn't crash
- [ ] Path validation: No map creates unreachable or blocked paths

### System Cohesion
- [ ] New maps integrate seamlessly with existing system
- [ ] No regressions in existing functionality (all tests pass)
- [ ] Visual consistency maintained (grid rendering unchanged)
- [ ] Gameplay balance preserved (difficulty distribution verified)

## Impact Analysis

### Directly Impacted (Validation Targets)
- `Sources/BugDefense/MapConfiguration.swift` - Contains all map definitions (primary validation target)
- `Sources/BugDefense/Bug.swift:240-302` - Waypoint movement system (verify unchanged)
- `Sources/BugDefense/GameScene.swift:488-504` - Bug spawning with path assignment (verify works with all maps)
- `Sources/BugDefense/GameScene.swift:826-856` - Tower placement blocking (verify works with all maps)
- `Sources/BugDefense/GameScene.swift:237-304` - Grid rendering (verify works with all maps)
- `Tests/BugDefenseTests/BugDefenseTests.swift` - Existing tests (verify all pass)

### Indirectly Impacted
- **Future gameplay:** More map variety increases replayability
- **Game balance:** Difficulty distribution affects player experience
- **Performance:** 30 maps vs 20 maps (should have negligible impact due to O(1) selection)
- **Documentation:** AI_PROMPT.md should be updated with validation results
- **Deployment readiness:** This validation determines if feature is production-ready

### Artifacts Created
- `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/TRACEABILITY_MATRIX.md` - Requirement verification documentation
- `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/PATTERN_ANALYSIS.md` - Map pattern quality assessment
- `/Users/jrc/Code/bug-defense/bug-defense-main/.claudiomiro/TASKΩ/VALIDATION_REPORT.md` - Final validation summary (optional)

## Follow-ups

### If Validation Passes
- Update first line of TODO.md to: `Fully implemented: YES`
- Document validation completion date
- Ready for production deployment
- Consider adding more maps in future iterations (optional)

### If Validation Fails
- **Fewer than 30 total maps:** Return to TASK1-10, complete missing implementations
- **Compilation errors:** Fix syntax errors in map definitions
- **Test failures:** Debug failing tests, fix invalid paths or logic errors
- **Path validation failures:** Correct paths that go out of bounds or don't reach house
- **Integration failures:** Debug MapManager, GameScene integration issues
- **Regression failures:** Identify and fix broken existing functionality

### Ambiguities Identified
- None identified at this stage (validation task has clear success criteria)
- If ambiguities arise during validation, document in this section and seek clarification

### Post-Validation Tasks (Out of Scope)
- Performance profiling with 30 maps (not required, system designed for O(1) selection)
- Additional map implementations beyond 10 new maps (not required for this feature)
- Dynamic map generation or user-created maps (future enhancement, not current scope)
- Map difficulty ratings in UI (future enhancement, not current scope)
